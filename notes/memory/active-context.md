# Active Context

- 2026-09-10: Dashboard rework (`notes/memory/dashboard-spec.md`) Phase 0 discovery done, read-only. Root cause of donut-vs-queue mismatch found: `fetch_agent_consensus_stats` counts divergence regardless of `overridden_at`; `fetch_hitl_exception_queue` excludes overridden rows. Not fixed yet — deferred to Phase 4 per spec. Awaiting go-ahead on Phase 1 (sidebar + selection state).
- Flagged: `app.py` (legacy Streamlit) calls `run_audit_committee` directly, bypassing `RunRiskAuditUseCase`/`record_audit_results` — never persists, dead-end path, don't build the new audit UI on it.
- 2026-09-10: Phase 1 done — `selectedCustomerId` in App.jsx now the single selection state (sidebar/search/registry all set it); drawer is a separate `openReportId` opened only via "Open full report". Dropdown removed from ChartsGrid. Awaiting go-ahead on Phase 2 (applicant card + portfolio/customer charts).
- 2026-09-10: Phase 2 done — applicant card, credit-score-band chart, Top-5 lists, customer-vs-portfolio comparison, and agent recommendations panel all built on real data (2 new read-only endpoints: `/api/dashboard/credit-score-bands`, `/api/dashboard/portfolio-highlights`, no schema change). Extracted `useCommitteeAudit`/`useCustomerProfile` hooks so the applicant card and the drawer share one audit-trigger handler (pulls part of Phase 3's "same handler" requirement forward). Phase 3 still owns: per-agent progress display, explicit "Try again" on failure, and verifying the double-submit/Ollama-down cases. Awaiting go-ahead on Phase 3.
- 2026-09-10: Bug found+fixed (user repro'd on customer #107): Agent Recommendations panel was blank right after a *fresh* audit run (worked fine on reload). Root cause: `AuditResultResponse` (the async task's completion payload) never carried `quant_verdict`/`qual_verdict`/`cro_verdict`/`*_basis` — those existed only in `SavedAuditResponse` (the DB-read shape). The same gap silently existed in `CustomerDrawer`'s "Full transcript" section pre-Phase-2 too, just hidden behind a collapsed `<details>`. Fixed by carrying `quant_basis`/`qual_basis`/`cro_basis` onto the `AuditResult` domain entity (`src/domain/entities.py`) from `run_risk_audit.py`'s already-computed `bases` dict, and populating the verdict fields in `run_audit_task.py` via the existing `QUANT_STANDING_TO_VERDICT`/`QUAL_ASSESSMENT_TO_VERDICT`/`CRO_DECISION_TO_VERDICT` maps (same ones `record_audit_results` uses) — no agent logic or DB schema touched, just plumbing already-computed data through. Verified live in Chrome: re-ran the committee for #107, panel populated immediately post-run.

# Skill: Modern Vite + Tailwind v4 Setup

- Enforce `@tailwindcss/postcss` for Tailwind v4 configurations.
- Do NOT generate `tailwindcss init -p` or legacy `tailwind.config.js` unless explicitly requested.
- CSS directive: `@import "tailwindcss";` in root CSS files.
- Ensure terminal commands check current working directory before chaining `cd frontend`.
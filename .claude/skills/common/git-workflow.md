# Skill: Git Workflow & Attribution

- Execute commits using standard `git commit -m "<type>: <description>"`.
- Do NOT override `user.name` or `user.email`.
- Do NOT include `Co-authored-by: Claude` or any AI attribution headers in the commit body.
- Verify unstaged changes with `git status --short` before committing to keep changes atomic.